Arbitray precision floats (or larger fixes precision e.g. qd)
Better threading -  ForkOS the openGL rendering 
Fix window resizing
Fix doubledisplay for each keypress

UI design:
	GUI
	button to reset position
	button to cycle through fractal types


Channel based IO and move the colour computation into the main threaded computation
